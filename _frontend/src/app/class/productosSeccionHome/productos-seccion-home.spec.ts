import { ProductosSeccionHome } from './productos-seccion-home';

describe('ProductosSeccionHome', () => {
  it('should create an instance', () => {
    expect(new ProductosSeccionHome()).toBeTruthy();
  });
});
